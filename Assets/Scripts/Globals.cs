public static class Globals
{
    public enum damageType { red, white, black, pale }
    public enum floorType { history, technology, literature, art }

    public static floorType selectedFloor = floorType.history;

    public static int currentMoney = 0;
}
